---
// src/pages/index.astro
import BaseLayout from '../layouts/BaseLayout.astro';

const response = await fetch('https://api.github.com/users/YoRzHe-HotaaRu');
const userData = await response.json();

const stats = {
  repos: userData.public_repos,
  followers: userData.followers,
  following: userData.following,
};
---

<BaseLayout title="YioRzHe">
  <div class="profile-header">
    <div class="profile-image">
      <img
        src={userData.avatar_url + "?s=120"}
        alt={userData.name || 'YioRzHe-HotaaRu'}
        width="120"
        height="120"
      />
    </div>
    <div class="profile-info">
		<h1><span class="gradient-text">{userData.name || 'YioRzHe-HotaaRu'}</span>☄️</h1>
		<p class="bio">
			{userData.bio || 'Developer, designer, and lifelong learner building cool things on the web.'}
		</p>

      	<!-- === Social Link === -->
		<div class="social-links">
		<a href="https://github.com/YoRzHe-HotaaRu" target="_blank" rel="noopener" class="social-link github">
			<i class="fab fa-github"></i>
			<span>GitHub</span>
		</a>
		<a href="https://linkedin.com/in/amir-hafizi-musa-5530b9364" target="_blank" rel="noopener" class="social-link linkedin">
			<i class="fab fa-linkedin"></i>
			<span>LinkedIn</span>
		</a>
		<a href="https://discord.com/users/330997221334056971" target="_blank" rel="noopener" class="social-link discord">
			<i class="fab fa-discord"></i>
			<span>Discord</span>
		</a>
		<a href="https://steamcommunity.com/id/amirhafizi177013" target="_blank" rel="noopener" class="social-link steam">
			<i class="fab fa-steam"></i>
			<span>Steam</span>
		</a>
		<a href="https://youtube.com/@VectorVulKan727" target="_blank" rel="noopener" class="social-link youtube">
			<i class="fab fa-youtube"></i>
			<span>YouTube</span>
		</a>
		</div>
    </div>
  </div>

  <!-- === GitHub Stats Section === -->
  <section class="stats-section">
    <h2>GitHub Stats</h2>
    <div class="stats-grid">
      <div class="stat-item">
        <span class="stat-number">{stats.repos}</span>
        <span class="stat-label">Repos</span>
      </div>
      <div class="stat-item">
        <span class="stat-number">{stats.followers}</span>
        <span class="stat-label">Followers</span>
      </div>
      <div class="stat-item">
        <span class="stat-number">{stats.following}</span>
        <span class="stat-label">Following</span>
      </div>
    </div>
  </section>

  	<!-- === Skills Section === -->
	<section class="skills-section">
	<h2>Experiences</h2>
		<div class="skills-row">
			
			<!-- GDScript -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/godotengine" alt="Godot" />
			<span>Godot</span>
			</div>

			<!-- Unity -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/unity" alt="Unity" />
			<span>Unity</span>
			</div>

			<!-- Unreal Engine -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/unrealengine" alt="Unreal Engine" />
			<span>Unreal</span>
			</div>

			<!-- Flask -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/flask" alt="FLask" />
			<span>Flask</span>
			</div>


			<!-- React -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/react" alt="React" />
			<span>React</span>
			</div>


			<!-- Astro -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/astro" alt="Astro" />
			<span>Astro</span>
			</div>

			<!-- Sass (SCSS) -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/sass" alt="Sass" />
			<span>SCSS</span>
			</div>


			<!-- Docker -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/docker" alt="Docker" />
			<span>Docker</span>
			</div>

			<!-- Ollama -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/ollama" alt="Ollama" />
			<span>Ollama</span>
			</div>
	
			<!-- Claude Code -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/anthropic" alt="Claude Code" />
			<span>Claude Code</span>
			</div>
		
			<!-- OpenAI Codex -->
			<div class="skill-icon">
			<img src="/assets/logo/openai.png" alt="OpenAI Codex" />
			<span>Codex</span>
			</div>
	
			<!-- OpenClaw -->
			<div class="skill-icon">
			<img src="/assets/logo/openclaw.png" alt="OpenClaw" />
			<span>OpenClaw</span>
			</div>
	
			<!-- Kilo Code -->
			<div class="skill-icon">
			<img src="/assets/logo/kilocode-color.png" alt="Kilo Code" />
			<span>Kilo Code</span>
			</div>
	
			<!-- Qwen -->
			<div class="skill-icon">
			<img src="/assets/logo/Qwen_logo.png" alt="Qwen Code" />
			<span>Qwen Code</span>
			</div>

			<!-- Wine -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/wine" alt="Wine" />
			<span>Wine</span>
			</div>

			<!-- Arch Linux -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/archlinux" alt="Arch Linux" />
			<span>Arch Linux</span>
			</div>

			<!-- Blende -->
			<div class="skill-icon">
			<img src="https://cdn.simpleicons.org/blender" alt="Blender" />
			<span>Blender</span>
			</div>
		</div>
	</section>

	<!-- === Recent Projects Section === -->
	<section class="recent-projects-section">
		<h2>Recent Projects</h2>
		<div class="carousel-wrapper">
			<div class="carousel-track">
				<button class="carousel-btn prev-btn" aria-label="Previous project">
					<i class="fas fa-chevron-left"></i>
				</button>
				<a href="https://nakano-room.vercel.app/" target="_blank" rel="noopener" class="carousel-slide active">
					<img src="/assets/recentProject/NakanoRoom.jpg" alt="Nakano Room" loading="lazy" />
					<div class="slide-label">Nakano Room</div>
				</a>
				<a href="https://vulkan747.pythonanywhere.com/" target="_blank" rel="noopener" class="carousel-slide">
					<img src="/assets/recentProject/akari_web.jpg" alt="A.K.A.R.I AI Assistant" loading="lazy" />
					<div class="slide-label">A.K.A.R.I AI Assistant</div>
				</a>
				<a href="https://pjima-perak-dashboard.pages.dev/" target="_blank" rel="noopener" class="carousel-slide">
					<img src="/assets/recentProject/PJIMA-Dashboard.jpg" alt="PJIMA Perak Dashboard" loading="lazy" />
					<div class="slide-label">PJIMA Perak Dashboard</div>
				</a>
				<a href="https://sksp.pages.dev/" target="_blank" rel="noopener" class="carousel-slide">
					<img src="/assets/recentProject/sksp_portal.jpg" alt="SKSP Portal Rasmi Sekolah" loading="lazy" />
					<div class="slide-label">SKSP Portal Rasmi Sekolah</div>
				</a>
				<a href="https://clarity-roadtag.vercel.app/" target="_blank" rel="noopener" class="carousel-slide">
					<img src="/assets/recentProject/roadtagvisualizer.jpg" alt="JKR Road Construction Tagging Visualizer" loading="lazy" />
					<div class="slide-label">JKR Road Construction Tagging Visualizer</div>
				</a>
				<a href="https://hydra-learn.pages.dev/" target="_blank" rel="noopener" class="carousel-slide">
					<img src="/assets/recentProject/guide_to_hydra.jpg" alt="Complete Guide to Hydra" loading="lazy" />
					<div class="slide-label">Complete Guide to Hydra</div>
				</a>
				<button class="carousel-btn next-btn" aria-label="Next project">
					<i class="fas fa-chevron-right"></i>
				</button>
			</div>
		</div>
		<div class="carousel-indicators">
			<span class="indicator active" data-index="0"></span>
			<span class="indicator" data-index="1"></span>
			<span class="indicator" data-index="2"></span>
			<span class="indicator" data-index="3"></span>
			<span class="indicator" data-index="4"></span>
			<span class="indicator" data-index="5"></span>
		</div>
	</section>
</BaseLayout>

<style>
  /* Lightweight idle animations - GPU optimized */

  /* Profile image gentle float + coin spin every 10s */
  .profile-image {
    animation: profileFloat 4s ease-in-out infinite;
    will-change: transform;
    perspective: 600px;
  }

  .profile-image img {
    animation: profileGlow 3s ease-in-out infinite, coinSpin 10s ease-in-out infinite;
    border-radius: 50%;
  }

  @keyframes profileFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }

  @keyframes profileGlow {
    0%, 100% { box-shadow: 0 4px 20px rgba(99, 124, 206, 0.3); }
    50% { box-shadow: 0 8px 30px rgba(99, 124, 206, 0.5), 0 0 40px rgba(99, 124, 206, 0.2); }
  }

  @keyframes coinSpin {
    0%, 80%, 100% { transform: rotateY(0deg); }
    85% { transform: rotateY(180deg); }
    90% { transform: rotateY(360deg); }
    95% { transform: rotateY(360deg); }
  }

  /* Username gradient shimmer + subtle float */
  .gradient-text {
    display: inline-block;
    animation: textFloat 5s ease-in-out infinite, textShimmer 4s linear infinite;
    background-size: 200% auto;
    will-change: transform, background-position;
  }

  @keyframes textFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-3px); }
  }

  @keyframes textShimmer {
    0% { background-position: 0% center; }
    100% { background-position: 200% center; }
  }

  /* Pause animations when user prefers reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .profile-image,
    .gradient-text {
      animation: none;
    }
  }
</style>

<style lang="scss">
  @use "sass:color";

  .recent-projects-section {
    margin-top: 2rem;
  }

  .recent-projects-section h2 {
    font-size: 1.5rem;
    color: #637cce;
    margin-bottom: 1.5rem;
    border-bottom: 1px solid #e0e0e0;
    padding-bottom: 0.5rem;
  }

  .carousel-wrapper {
    position: relative;
  }

  .carousel-track {
    position: relative;
    width: 100%;
    height: 420px;
    border-radius: 0;
    overflow: hidden;
    outline: 2px solid #637cce;
    outline-offset: 4px;
    box-shadow: 8px 8px 0 #637cce, 12px 12px 30px rgba(0, 0, 0, 0.2);
    background: #1a1a2e;
  }

  .carousel-slide {
    position: absolute;
    inset: 0;
    opacity: 0;
    transform: scale(1.05);
    transition: opacity 0.7s ease, transform 0.7s ease;
    display: block;
    text-decoration: none;
    cursor: pointer;

    &.active {
      opacity: 1;
      transform: scale(1);
      z-index: 2;
    }

    &.slide-out-left {
      opacity: 0;
      transform: translateX(-40px) scale(0.95);
      z-index: 1;
    }

    &.slide-out-right {
      opacity: 0;
      transform: translateX(40px) scale(0.95);
      z-index: 1;
    }

    &.slide-in-left {
      animation: slideInFromLeft 0.7s ease forwards;
    }

    &.slide-in-right {
      animation: slideInFromRight 0.7s ease forwards;
    }

    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }

  @keyframes slideInFromLeft {
    from { opacity: 0; transform: translateX(-40px) scale(0.95); }
    to { opacity: 1; transform: translateX(0) scale(1); }
  }

  @keyframes slideInFromRight {
    from { opacity: 0; transform: translateX(40px) scale(0.95); }
    to { opacity: 1; transform: translateX(0) scale(1); }
  }

  .slide-label {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1rem 1.5rem;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.85));
    color: #fff;
    font-size: 1.15rem;
    font-weight: 600;
    letter-spacing: 0.02em;
  }

  .carousel-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 44px;
    height: 44px;
    border-radius: 0;
    border: none;
    background: rgba(0, 0, 0, 0.4);
    color: #fff;
    font-size: 1.1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.25s ease;
    z-index: 5;
    backdrop-filter: blur(4px);

    &:hover {
      background: rgba(99, 124, 206, 0.8);
      color: #fff;
    }

    &.prev-btn {
      left: 0;
    }

    &.next-btn {
      right: 0;
    }
  }

  .carousel-indicators {
    display: flex;
    justify-content: center;
    gap: 0.6rem;
    margin-top: 1.25rem;
  }

  .indicator {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #cdd5f0;
    cursor: pointer;
    transition: all 0.35s ease;

    &.active {
      background: #637cce;
      transform: scale(1.3);
      box-shadow: 0 0 8px rgba(99, 124, 206, 0.5);
    }

    &:hover:not(.active) {
      background: #a0b0e0;
    }
  }

  @media (max-width: 768px) {
    .carousel-track {
      height: 260px;
    }
    .carousel-btn {
      width: 36px;
      height: 36px;
      font-size: 0.9rem;
    }
    .slide-label {
      font-size: 0.95rem;
      padding: 0.75rem 1rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const slides = Array.from(document.querySelectorAll('.carousel-slide'));
    const indicators = Array.from(document.querySelectorAll('.indicator'));
    const prevBtn = document.querySelector('.prev-btn') as HTMLButtonElement | null;
    const nextBtn = document.querySelector('.next-btn') as HTMLButtonElement | null;
    let current = 0;
    let autoTimer: ReturnType<typeof setInterval>;
    const INTERVAL = 5000;
    const total = slides.length;

    function goTo(index: number, direction: 'left' | 'right') {
      if (index === current) return;
      const oldSlide = slides[current];
      const newSlide = slides[index];

      oldSlide.classList.remove('active');
      oldSlide.classList.add(direction === 'left' ? 'slide-out-left' : 'slide-out-right');

      newSlide.classList.remove('slide-out-left', 'slide-out-right');
      newSlide.classList.add('active', direction === 'left' ? 'slide-in-right' : 'slide-in-left');

      indicators[current].classList.remove('active');
      indicators[index].classList.add('active');

      current = index;

      setTimeout(() => {
        oldSlide.classList.remove('slide-out-left', 'slide-out-right');
        newSlide.classList.remove('slide-in-left', 'slide-in-right');
      }, 750);

      resetAuto();
    }

    function next() {
      goTo((current + 1) % total, 'left');
    }

    function prev() {
      goTo((current - 1 + total) % total, 'right');
    }

    function startAuto() {
      autoTimer = setInterval(next, INTERVAL);
    }

    function resetAuto() {
      clearInterval(autoTimer);
      startAuto();
    }

    nextBtn?.addEventListener('click', next);
    prevBtn?.addEventListener('click', prev);

    indicators.forEach((dot) => {
      dot.addEventListener('click', () => {
        const idx = parseInt((dot as HTMLElement).dataset.index || '0', 10);
        if (idx > current) goTo(idx, 'left');
        else if (idx < current) goTo(idx, 'right');
      });
    });

    startAuto();
  });
</script>